# Installation

# From source

to use the [pull paper jar script](), you must first install jq. You can do so easily using
```sh
brew install jq
```
change the variable VERSION with the version you want to pull.